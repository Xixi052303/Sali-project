# 核心循环与单局结构

## 核心循环

**章节状态：核心循环和六区操作已确认；精确事件排程待定。**

```text
开局获得土豆
        ↓
自动前进、自动投喂，连续横移绕行与选门
        ↓
高频普通强化提高全局厨房或餐车属性
        ↓
遭遇静止且横跨六区的精英
        ↓
满足精英，获得特殊三选一
        ↓
约第五分钟进入中途Boss战，停止前进
        ↓
胜利后获得局内特殊强化并恢复前进
        ↓
继续普通门、普通食客与精英
        ↓
进入最终Boss战，停止前进
        ↓
结算通关与首胜内容解锁
```

## 玩家输入与六区场地

- 按住并横向拖动时，厨师和餐车连续跟随手指横坐标移动。
- 松开手指后保持当前位置，不自动回中。
- 玩家不能主动控制前进速度、瞄准、普通攻击或释放技能。
- 场地横向划分为六个逻辑路线区域，用于生成、占位、强化门和危险预警判定。
- 六区不等于硬车道；餐车不会吸附区域中心，玩家可以在区域间连续移动。
- 普通强化门的左右选项原则上各占三个逻辑区域。

## 自动攻击与目标选择

- 所有食材独立按自身间隔自动攻击。
- 默认目标是前方有效范围内距离餐车最近的未满足食客、可攻击普通门面板或餐车当前横向位置能够取得的食客奖励门。
- 餐车位于门的左半区时只把左侧门作为候选，位于右半区时只把右侧门作为候选；当前侧达到上限后不会自动转攻另一侧。
- 餐车越过门线时严格只结算一个选项；中心点归右侧，不存在同时取得两侧效果的判定区。
- 食客奖励门只占原食客对应的横向区域；餐车必须经过该区域才取得奖励，绕开即错过。
- 同距离时优先选择更接近餐车横坐标的目标，再按进入场景的先后顺序稳定裁决。
- 横向移动会改变最近目标和弹道方向，是玩家调整优先目标的主要手段。
- 没有有效目标时仍按各食材间隔朝正前方自动攻击；需要目标角度的食材仅在存在有效目标时改用目标方向，不积存攻击轮次。

## 单局结构

**章节状态：八分钟、约 50 次普通门、6 只精英和 2 场 Boss 已确认；精确时间点与时长待定。**

- 单局目标时长约 8 分钟。
- 前进阶段目标生成约 50 次普通强化门；普通门数量优先于早期“约 10 秒一次”的粗略间隔。
- 普通强化门只在前进阶段生成，两场 Boss 战期间停止。
- 每局共出现 6 只精英和 2 场 Boss，合计承担 8 次特殊强化或内容解锁节点。
- 中途 Boss 约在第五分钟出现；最终 Boss 位于关卡末段。
- Boss 登场后停止向前移动，Boss 结束后中途战恢复前进，最终战进入结算。

### 当前竖切片排程约束

- 普通食客、精英与普通门可以同屏，但纵向中心必须保留约`180px`安全距离；预计会在抵达餐车前追尾时，后请求的对象延迟出现。
- 精英事件开始后暂停普通食客波次，特殊选择结束后约`1s`恢复。
- 当前竖切片精英后的六道门约在`86、94、102、110、118、126s`请求出现；若仍有占位冲突则保留请求，安全后再生成。

精确事件排程见[开放问题](open-questions.md#单局结构)。
