# “小厨西”Godot 项目协作规则

## 适用范围与优先级

- 本文件适用于项目根目录及全部子目录；通用交流、代码注释、Git 安全和提交文案格式遵循上级协作规则。
- 指令优先级依次为：用户当前明确要求、用户已确认的项目设计、本文件、项目现有实现与局部约定、通用最佳实践。
- 设计资料与当前源码冲突时，分别说明“现行设计”和“当前实现”，不得擅自用任一方覆盖另一方。
- 修改前读取直接相关的事实源，只做任务涉及的最小变更；不确定的结论必须标明证据不足或需要确认。

## 项目基线

- 引擎为 Godot 4.7，主要语言为静态类型 GDScript，渲染方式为 Mobile。
- Godot 4.7 编辑器：`D:\SteamLibrary\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe`
- 项目是竖屏移动端直接 3D 纸艺玩法原型，内部设计分辨率为 `720 × 1280`，移动端锁定竖屏。
- 唯一启动场景为 `res://scenes/run_3d.tscn`，当前没有 Autoload；旧 2D 场景已移除。
- 当前导出目标为 Android ARM64 原型，配置见 `export_presets.cfg`。
- 当前美术主要由可编辑 3D 临时节点与少量贴图构成；没有用户要求时，不把占位表现误认作正式美术或顺手整套替换。

除非用户明确要求升级或兼容其他版本，否则代码、场景、资源与设置均以 Godot 4.7 为准。

## 强制 Skill 路由

- 任何代码、Godot 场景、资源装配、信号、运行状态、数据链路或工程配置任务，先使用 `food-td-engineering-lead`；由它恢复调用链、选择验证并协调 Godot 专项 Skill。
- Godot 架构使用 `godot`，`.gd` 使用 `godot-gdscript`，Shader 和材质使用 `godot-shader`；只有实际引入对应技术后才使用 `godot-csharp` 或 `godot-gdextension`。
- 设计讨论、GDD 维护、跨系统协调和决策记录使用 `food-td-lead-designer`，不得替用户裁决未定规则。
- 项目出现人工维护的 `.xlsx` 时，业务内容由对应领域决定，写入同时使用 `food-td-excel-workbook-maintainer`；不得预设旧项目的工作簿路径或导出链。
- 概念图、位图生成或图片编辑使用 `imagegen`；资源接入仍走技术负责人和对应 Godot Skill。
- 首次读取 `.agents/skills/` 下的项目 Skill 及其引用资料时显式使用 UTF-8；出现乱码时先检查编码，不在乱码状态下修改。

不存在的专项 Skill 只能列为潜在依赖，不能假装已经调用，也不能借其名义给出专业结论。

## 设计事实与状态

- `docs/策划案.md` 是稳定入口；设计任务先完整读取 `docs/gdd/index.md`，再读取任务对应的唯一领域事实源。
- `docs/gdd/open-questions.md` 中的待定、候选和待验证事项不是制作规格。
- `docs/gdd/decision-log.md` 只记录历史，不继续指导当前实现。
- `balance_tables/时间轴.xlsx`、`战斗规则.xlsx`、`食客.xlsx`、`武器.xlsx`、`普通强化.xlsx`和`特殊强化.xlsx`保存对应领域的当前运行参数；`data/**/*.tres`与脚本默认值保存安全回退。精确运行值还需核对Excel适配器、Resource类型和调用处。
- `project.godot`、`scenes/` 与 `scripts/` 是当前可运行实现的事实源；README 只作运行与验证入口，不替代 GDD 或源码。
- 每条完整设计规则只在一个领域文档中维护；索引和其他文档只写摘要与链接。

当前高可信设计支柱是：一指操作、高频成长、简单食客与鲜明检查、料理构筑、荒诞温暖。

现行 GDD 与当前可运行实现均采用约 8 分钟的正式流程骨架：50 次普通强化门、250 个普通波次（按当前每四波一次双客约 312 只普通食客）、6 只精英和 2 场 Boss。尚未完成的正式内容、表现与局外系统仍须标为制作目标，不得把流程骨架已接入误写成全部设计均已实现。

## 不可破坏的边界

- 六区是逻辑判定区域，不是吸附车道；普通阶段玩家只控制连续横移，Boss阶段可在开战站位与Boss之间有限二维移动；前进、瞄准和攻击保持自动。
- 当前普通食材在发射瞬间确定方向且默认不追踪；瞄准发射与飞行追踪是独立能力。
- 同类持久百分比普通强化线性加算，快速备餐以累计攻速反推攻击间隔，固定点数餐车强化继续加算；攻击间隔下限为 `1/60s`。
- 餐车耐久是唯一失败资源，受击后有 0.5 秒无敌；不新增未获确认的生命或投诉计量。
- 食客被满足后满意离场，不使用死亡、尸体或真实暴力反馈。
- 当前时间轴、战斗规则、食客、武器、普通强化和特殊强化使用轻量Excel运行时读取链；仍没有JSON导出器、资源生成器或对象池。不得照搬旧项目的数据管线，也不为局部任务提前引入全局单例或批量生成链。
- 修改场景时保留现有 UID、节点唯一名称和外部资源引用；`.uid`、`.import` 和 `.godot/` 由 Godot 管理，不手工伪造或编辑。

工程实现细则、职责导航、数据所有权、验证矩阵和用户验收格式由 `food-td-engineering-lead` 维护，不在本文件重复展开。

## 验证与文件安全

- 工程改动按技术负责人 Skill 选择最小充分验证；无法启动 Godot 4.7 时明确列出未执行项，不用静态检查冒充运行通过。
- 输入、HUD、边界或表现改动必须覆盖竖屏设计尺寸和目标移动端比例。
- Codex 运行测试或视觉验证生成的截图、录屏、音频、日志、渲染结果和其他中间文件必须直接写入项目根目录的 `tmp/`；不得先输出到项目根目录或资源目录再清理，避免触发 Godot 资源导入。
- Codex 不得在受限沙箱内启动任何 Godot 进程，包括核心测试、烟雾测试、专项脚本、导入扫描、编辑器启动和可视验证。本机已确认 Steam 版 Godot 4.7 即使使用 `--headless` 并隔离用户目录，仍可能在退出阶段触发 `godot_runner.exe` / `godot.windows.opt.tools.64.exe` 原生内存读取崩溃。
- Godot 运行验证只能在用户批准后，以非沙箱方式直接启动 Godot 4.7 官方控制台程序；无画面验证必须同时使用 `--headless`，并从进程启动前把该次运行的 `APPDATA`、`LOCALAPPDATA` 和 `--log-file` 隔离到 `tmp/` 下互不复用的路径。不得读写正式 `user://logs`，不得用 Steam GUI 程序、复制或重命名后的 `godot_runner.exe`、临时包装程序替代控制台程序，也不得因找不到控制台程序自动降级到其他 Godot 可执行文件。
- 无法获得非沙箱执行批准或找不到准确的 Godot 4.7 控制台程序时，停止运行验证，只做静态检查，并在交付中明确列为“未执行”；不得为了追求测试结果绕过该限制。
- Godot 自动测试必须串行执行并等待各自退出；只管理本次命令启动并记录的进程 ID，不复用、终止或按名称批量清理用户已经打开的 Godot 编辑器及其他 Godot 进程。
- 只有必须核对实际画面时才启动带窗口的 Godot。可视验证同样必须经用户批准后在非沙箱环境运行，并通过命令行临时使用 Vulkan 与 Mobile，避开本机已有 `dxgi.dll / D3D12` 崩溃记录；不得为测试静默修改 `project.godot`。任务若专门验证 D3D12，则明确说明风险后使用项目配置。
- 单独出现 `Failed to read the root certificate store` 只有在进程正常退出且目标成功标志存在时才可作为非阻断环境告警；若同时出现应用程序错误弹窗、异常退出、`signal 11` 或缺少成功标志，必须判定运行验证失败，保留该次 `tmp/` 日志、退出状态和复现条件，且不得在同一执行环境连续重试。
- 没有用户要求时不构建、签名或覆盖 APK，不运行批量写盘流程。
- 修改前检查工作区状态并保留用户已有改动；无法安全区分重叠修改时先报告。
- 不提交 `.godot/`、`build/`、`android/`、`tmp/`、导出产物、凭据或疑似密钥。
- 不自动执行 `commit`、`push`、发布或创建远程仓库，除非用户明确要求。
